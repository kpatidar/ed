<center>
<div class="row">
  <center><h2>ALL ALBUMS</h2></center>
   <% @albums.each do |album| %>
    <div class="col1">
      <div class="album_info">
        <font color="blue">album <%= album.album_name%></font>
      </div>
      <% if album.cover_photo %>
	<%= link_to image_tag( album.cover_photo.image.url(:original) , :height => "200" , :width => "200" , :title => "Show photos") , album_photos_path(album)  %>
      <% else %>
        <%= link_to image_tag( '/images/admin.jpeg' , :height => "200" , :width => "200" , :title => "Show photos") , album_photos_path(album)  %>
      <% end %>
      <div class="link">
          <font color="blue"><%= album.album_name %> by <%= album.user.name %></font>
      </div> 
      <div class="delete"><%= ( link_to image_tag("/images/delete_img.jpeg" ,:height => "20" , :width => "20" , :title => "Delete album"), album_path(album), confirm: 'Are you sure?', method: :delete ) if (album.user_id == current_user.id) or is_admin?%></div>
    </div>
   <% end %>
</div>
</center>