<h1>Listing photos</h1>

<table>

<% @photos.each do |photo| %>
  <tr>
    <td><%= photo.album_id %></td>
    <td> <%= image_tag photo.image.url(:medium), :height => "200" , :width => "200" %></td>
    <td><%= link_to 'Download' %> </td>
    <td><%= link_to 'Show', album_photo_path(photo.album,photo) %></td>
  </tr>
<% end %>
</table>

<br />
